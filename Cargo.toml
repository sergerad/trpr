[package]
name = "kwkly"
version = "0.1.0"
edition = "2021"
description = "Local daemon that watches your GitHub PRs for new comments and spawns headless Claude Code runs to propose diffs for local review"

[dependencies]
anyhow = "1"
chrono = { version = "0.4", features = ["serde"] }
dirs = "5"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
toml = "0.8"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
